{
  "name": "tfs-bracket-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud Functions for tfs-bracket production safety (budget alerts, usage spike kill switch)",
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.0"
  }
}
